/**
 * Default filesystem paths used by init scaffolding and the add command.
 * Keep init output aligned with detect-project so `robloxui add` works out of the box.
 */

/** Where marketplace components are installed (relative to project root). */
export const DEFAULT_COMPONENTS_PATH = "src/client/ui/components";
